#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
	char v[6];
	int vet[6];
	while(scanf("%s",v)!=0)
	{
		tam = strlen(v);
				for(int i = 0; i < tam; i++)
				{
					
				}
	}
	return 0;
}